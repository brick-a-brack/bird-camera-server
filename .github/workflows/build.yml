name: Build

on:
  push:
    branches:
      - master
      - dev
  workflow_dispatch:

jobs:
  # ---------------------------------------------------------------------------
  # macOS — Universal Binary (x86_64 + arm64)
  # The Canon EDSDK.framework ships as a universal binary, so a single output
  # binary works natively on both Intel and Apple Silicon Macs.
  # ---------------------------------------------------------------------------
  build-macos:
    name: Build (macOS universal)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust targets
        run: rustup target add x86_64-apple-darwin aarch64-apple-darwin

      # TODO: Install Canon EDSDK (macOS)
      # The SDK is git-ignored and must be fetched from a private source.
      # Place the framework at:
      #   external/EDSDK/EDSDKv132010M/EDSDK.framework
      # Example using a secret download URL:
      #   curl -fsSL "${{ secrets.CANON_SDK_MACOS_URL }}" -o canon_sdk.zip
      #   unzip -q canon_sdk.zip -d external/EDSDK/
      - name: TODO — Install Canon EDSDK
        run: echo "Canon SDK installation not yet configured — see TODO in workflow"

      - name: Build x86_64
        run: |
          cargo build --release \
            --target x86_64-apple-darwin \
            --features backend-webcam-macos,backend-canon

      - name: Build arm64
        run: |
          cargo build --release \
            --target aarch64-apple-darwin \
            --features backend-webcam-macos,backend-canon

      - name: Create universal binary with lipo
        run: |
          lipo -create \
            target/x86_64-apple-darwin/release/bird-camera-server \
            target/aarch64-apple-darwin/release/bird-camera-server \
            -output bird-camera-server-macos

      - name: Package into zip
        run: |
          mkdir -p dist/bird-camera-server-macos

          cp bird-camera-server-macos dist/bird-camera-server-macos/bird-camera-server

          # Add a relative rpath so the binary finds EDSDK.framework next to itself
          # when run outside the build tree.
          install_name_tool \
            -add_rpath @executable_path \
            dist/bird-camera-server-macos/bird-camera-server

          # Canon framework — only if present (SDK installed in CI)
          FRAMEWORK_SRC="external/EDSDK/EDSDKv132010M/EDSDK.framework"
          if [ -d "$FRAMEWORK_SRC" ]; then
            cp -R "$FRAMEWORK_SRC" dist/bird-camera-server-macos/
          fi

          cd dist
          zip -r bird-camera-server-macos.zip bird-camera-server-macos/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bird-camera-server-macos
          path: dist/bird-camera-server-macos.zip
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # Windows — x86_64
  # Canon EDSDK only ships for x86_64 on Windows. ARM64 Windows users can run
  # the x86_64 binary via the built-in x64 emulation layer.
  # ---------------------------------------------------------------------------
  build-windows:
    name: Build (Windows x86_64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Install Canon EDSDK (Windows)
      # The SDK is git-ignored and must be fetched from a private source.
      # Place the files at:
      #   external/EDSDK/EDSDKv132010W/Windows/EDSDK_64/Library/EDSDK.lib
      #   external/EDSDK/EDSDKv132010W/Windows/EDSDK_64/Dll/EDSDK.dll
      #   external/EDSDK/EDSDKv132010W/Windows/EDSDK_64/Dll/EdsImage.dll
      # Example using a secret download URL:
      #   Invoke-WebRequest -Uri "$env:CANON_SDK_WINDOWS_URL" -OutFile canon_sdk.zip
      #   Expand-Archive canon_sdk.zip -DestinationPath external\EDSDK\
      - name: TODO — Install Canon EDSDK
        run: echo "Canon SDK installation not yet configured — see TODO in workflow"

      - name: Build x86_64
        run: |
          cargo build --release `
            --target x86_64-pc-windows-msvc `
            --features backend-webcam-windows,backend-canon

      - name: Package into zip
        shell: pwsh
        run: |
          $out = "dist\bird-camera-server-windows"
          New-Item -ItemType Directory -Force -Path $out | Out-Null

          Copy-Item "target\x86_64-pc-windows-msvc\release\bird-camera-server.exe" $out

          # Canon DLLs — only if they were copied by build.rs (SDK present)
          $dlls = @("EDSDK.dll", "EdsImage.dll")
          foreach ($dll in $dlls) {
            $src = "target\x86_64-pc-windows-msvc\release\$dll"
            if (Test-Path $src) {
              Copy-Item $src $out
            }
          }

          Compress-Archive -Path "$out\*" -DestinationPath "dist\bird-camera-server-windows.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bird-camera-server-windows
          path: dist/bird-camera-server-windows.zip
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # Attach to current draft release
  # Runs on master/dev pushes. Finds the most recent draft release and uploads
  # built binaries to it. Does nothing if no draft release exists.
  # ---------------------------------------------------------------------------
  release:
    name: Attach to draft release
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: bird-camera-server-macos
          path: dist/

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: bird-camera-server-windows
          path: dist/

      - name: Attach assets to current draft release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          DRAFT_TAG=$(gh release list \
            --repo "$GITHUB_REPOSITORY" \
            --limit 100 \
            --json tagName,isDraft,createdAt \
            --jq 'map(select(.isDraft == true)) | sort_by(.createdAt) | last | .tagName // empty')

          if [ -n "$DRAFT_TAG" ]; then
            echo "Draft release found ($DRAFT_TAG) — uploading assets"
            gh release upload "$DRAFT_TAG" \
              dist/bird-camera-server-macos.zip \
              dist/bird-camera-server-windows.zip \
              --repo "$GITHUB_REPOSITORY" \
              --clobber
          else
            echo "No draft release found — skipping upload"
          fi
